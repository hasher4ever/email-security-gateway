# Score-to-action mapping. In shadow mode (GATEWAY_MODE=shadow) the milter
# headers are still added but Postfix delivers regardless — admins read the
# X-Spam-* headers in the BCC mailbox to tune.
reject = 15;
add_header = 6;
greylist = 4;
